/** Stable wire errors for browser/server authenticated-identity consistency. */
export const AUTH_IDENTITY_ERRORS = {
  CHANGED: 'auth_identity_changed',
  EXPECTATION_REQUIRED: 'auth_identity_expectation_required',
} as const;
